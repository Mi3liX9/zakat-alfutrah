---

interface Props {
    href: string;

}

const {href} = Astro.props

const path = Astro.url.pathname
console.log(path);


---

<a href={href} class={`text-2xl ${path === href ? "my-yellow font-bold" : "text-gray-300"}`}><slot /></a>
